# study-hongong-mldl
'혼자 공부하는 머신러닝+딥러닝(한빛미디어)' 학습 기록
## 사용 기술 및 도구
- **Language** : python
- **Libraries** : scikit-learn, pandas, numpy, matplotlib

## 학습 로그
### 2026.05.04. Chapter 06-1. 군집 알고리즘
* 비지도 학습 맛보기
* 표준 사과 만들기 apple_mean
* 오차 지도 만들기 abs_diff
* 오차 점수 매기기 abs_mean
* 범인 검거하기 argsort

---
### 2026.05.02. Chapter 05-3.트리의 앙상블
* 앙상블 : 하나의 강력한 모델을 만드는 대신, 여러 개의 평범한 모델을 학습시킨 뒤 그 결과를 합쳐서 더 정확한 예측을 하는 방법
* 얕은 트리 사용 → 순차적 학습 → 결합
  - 랜덤 포레스트, 엑스트라 트리, 그레이디언트 부스팅, 히스토그램 기반 그레이디언트 부스팅

* 랜덤 포레스트
  - 부트스트랩 샘플 사용, 가장 좋은 분할 찾음
  - 가장 대표적인 앙상블 학습 알고리즘, 성능이 좋고 안정적
* 엑스트라 트리
  - 부트스트랩 샘플 사용 X, 결정 트리의 노드를 랜덤하게 분할
* 그레이디언트 부스팅
  - 이전 트리의 손실을 보완하는 식으로 얕은 결정 트리를 연속하여 추가
* 히스토그램 기반 그레이디언트 부스팅
  - 훈련 데이터를 256개의 정수 구간으로 나눠 빠르고 높은 성능
  - 가장 뛰어난 앙상블 학습으로 평가받음
* 지금까지는 입력과 타깃이 준비된 문제 = 지도학습
  
---
### 2026.04.29. Chapter 05-2. 교차 검증과 그리드 서치
* 훈련 세트를 나눠 **검증 세트** 만들기
* cross_validate(교차검증) : 검증 세트를 떼어 내어 평가하는 과정을 여러 번 반복
  - 훈련 세트를 섞는 기준 = 분할기 지정
  - 회귀 모델 $→$ KFold 분할기
  - 분류 모델 $→$ StratifiedKFold
* 하이퍼파라미터 튜닝
  - 그리드 서치 GridSearchCV : 교차 검증으로 하이퍼파라미터 탐색 수행
  - 랜덤 서치 RandomizedSearchCV : 교차 검증으로 **랜덤한** 하이퍼파라미터 수행
* 교차 검증 수행 후 아래정보들을 저장
  - 최적의 매개변수 조합 : .best_params_
  - 최적의 매개변수 조합으로 훈련된 모델 : .best_estimator_
* 마지막으로 테스트 세트 점수 확인

---
### 2026.04.28. Chapter 05-1. 결정트리
* DecisionTreeClassifier 클래스 
* 예/아니오에 대한 질문을 이어나가면서 정답을 찾아 학습하는 알고리즘
* 불순도 : 결정 트리가 최적의 질문을 찾기 위한 기준(
  - 사이킷런은 criterion의 값으로 지니(기본값), 엔트로피 불순도 제공
* 정보 이득 : 부모 노드와 자식 노드의 불순도 차이, 자식이 순수할수록 높음
* 결정 트리는 제한 없이 성장하는 경우 과대적합 되기 쉬워, max_depth로 가지치기(성장 제한) 필요
* 특성 중요도는 결정 트리에 사용된 특성이 불순도 감소에 기여한 정도, feature_important_에 저장됨)

---
### 2026.04.23. Chapter 04-2. 확률적 경사 하강법
* 손실 함수를 따라 조금씩 내려가면서 손실이 가장 적은 지점을 찾는 알고리즘

---
### 2026.04.22. Chapter 04-1. 로지스틱 회귀
* 로지스틱 회귀는 사실 선형 방정식을 사용하는 **분류 모델**
* 이진 분류
  - 하나의 선형 방정식 훈련 → 시그모이드 함수에 통과시켜 0~1 사이의 값을 만듦(=양성 클래스에 대한 확률)
  - 음성 클래스에 대한 확률 = 1- 양성 클래스에 대한 확률
* 다중 분류
  - 클래스 개수만큼 방정식을 훈련 → 소프트맥스 함수에 통과시켜 전체 클래스의 합이 항상 1이 되도록 만듦 
  - 각 클래스에 대한 확률 계산
* 시그모이드와 소프트맥스는 나중에 신경망 단계에서 또 나옴!

---
### 2026.04.20. Chapter 03-3.특성공학과 규제
**특성공학(늘리기)  →  규제 적용(릿지/라쏘)  →  강도 조절(alpha)  →  최적 모델 완성!**
* 데이터 특성 늘리기(PolynomialFeatures) $\rightarrow$ 특성을 무작정 늘리면 모델이 복잡해져 **과대적합** 발생
* 이를 해결하기 위 계수($w$)를 제어하는 **규제 적용** $\rightarrow$ 릿지/라쏘 회귀
  - 릿지 : 계수($w$)를 작게 만들어 모델을 부드럽게, 모든 특성을 다 안고감
  - 라쏘 : 중요하지 않은 계수($w$)를 0으로 만들어 쓸모없는 특성을 가차 없이 버림
* 하이퍼파라미터($\alpha$)를 설정하여 규제 강도를 조절
